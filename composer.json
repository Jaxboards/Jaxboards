{
  "config": {
    "platform": {
      "php": "7.2.32"
    }
  },
  "scripts": {
    "post-install-cmd": "phpcs --config-delete installed_paths && phpcs --config-set installed_paths $(pwd)/vendor/phpcompatibility/php-compatibility",
    "lint": [
      "@phpcs"
    ],
    "phpcs": "phpcs",
    "phpcbf": "phpcbf"
  },
  "require-dev": {
    "phpcompatibility/php-compatibility": "^9.3",
    "roave/security-advisories": "dev-master@dev",
    "squizlabs/php_codesniffer": "^3.5"
  },
  "require": {
    "php": ">=7.2.32"
  }
}
