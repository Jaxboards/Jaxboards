#!/usr/bin/env php
<?php

use DI\Container;
use Tools\Index;

// Load composer dependencies.
require_once __DIR__ . '/vendor/autoload.php';

$container = new Container();

$container->get(Index::class)->route(array_slice($argv ?? [], 1));
